{
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "https://music-api.linust.org/:match*"
    }
  ],
  "build": {
    "env": {
      "NODE_OPTIONS": "--openssl-legacy-provider"
    }
  }
}
